# =========================================================
# 1. CORE DATA SCIENCE & I/O (Sử dụng trong môi trường Production)
# =========================================================
# Thư viện tính toán khoa học cơ bản
numpy>=1.24.3
pandas>=2.0.3

# ENGINE BẮT BUỘC CHO I/O
# Engine đọc/ghi file Parquet (.parquet)
pyarrow>=10.0.0
fastparquet>=2023.10.1
# Engine để đọc file Excel (.xlsx)
openpyxl>=3.1.2

# Phân tích thống kê (Dùng cho Drift Detection)
scipy>=1.11.0

# =========================================================
# 2. MACHINE LEARNING MODELS
# =========================================================
# Thư viện ML chính
scikit-learn>=1.3.0
# Model Gradient Boosting
xgboost>=2.0.0
# Hỗ trợ SMOTE và ImbPipeline (Xử lý dữ liệu mất cân bằng)
imbalanced-learn>=0.11.0

# =========================================================
# 3. VISUALIZATION & EXPLAINABILITY
# =========================================================
matplotlib>=3.7.2
seaborn>=0.12.2
# Giải thích lý do tại sao khách hàng Churn (SHAP analysis)
shap>=0.42.1

# =========================================================
# 4. UTILITIES & SYSTEM
# =========================================================
# Đọc file cấu hình config.yaml
PyYAML>=6.0.1
# Lưu/Tải model (.pkl) và transformer state (.joblib)
joblib>=1.3.2
# Thanh tiến trình
tqdm>=4.66.1
# Hỗ trợ Type Hinting
typing-extensions>=4.7.1


# =========================================================
# 5. DEVELOPMENT & TESTING (Nên tách ra file requirements-dev.txt)

pytest>=9.0.0
pytest-cov>=7.0.0